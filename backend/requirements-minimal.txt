# Core API dependencies (minimal for production)
fastapi==0.104.1
uvicorn[standard]
pydantic==2.5.0
python-dotenv
python-multipart

# Database
supabase
psycopg2-binary

# Data processing (lightweight)
numpy
pandas

# LLM (minimal - only what's needed)
huggingface-hub
langchain-core
langchain-community
langchain
langchain-huggingface

# Vector DB and RAG (lightweight model)
upstash-vector
sentence-transformers

# Utilities
requests
aiohttp
